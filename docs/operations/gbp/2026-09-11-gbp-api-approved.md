# GBP API アクセス申請が承認された（2026-09-10 受信）

- 確認日：2026-09-11（Gmail 読み取り・`scripts/gmail-search.mjs --preset=gbp`）
- 受信日時：2026-09-10 06:32 PDT ＝ **2026-09-10 22:32 JST**
- 差出人：`googlebusinessprofile-support@google.com`
- 件名：`[8-8661000041490] ビジネス プロフィールのサポートに関するお問い合わせについて`
- 対応する申請：2026-08-21 の再申請（ケース番号が一致。経緯は `2026-08-19-gbp-api-reapplication.md`）

---

## 1. 承認内容

> Congratulations! We have reviewed your allowlist request, and your project ( 487251905710 ) has been approved to use the Google Business Profile API. Your project has been granted a default quota of 300 Queries Per Minute (QPM).

| 項目 | 内容 |
|---|---|
| 結果 | **承認** |
| 対象プロジェクト | `487251905710` |
| 付与された割り当て | **300 QPM**（デフォルト） |

2026-08-18 に届いた却下（ケース `2-9362000041843`、理由「内部品質チェック不通過・承認済みプロジェクトなし・リスティングIDの紐付け」）から、8/21 の是正・再申請を経て承認に至った。**割り当て0の状態は解消。**

## 2. Google が求める残りの手順（メール本文より）

1. **Cloud Console で API を有効化する**（"Please ensure the API is activated in your Google Cloud Console"）
2. Google Workspace 利用時は組織側で Business Profile を有効化する（当社は Gmail アカウント運用のため該当しない見込み）

### ポリシー上の注意

メール本文に明記：**Google との提携・スポンサー・推奨を示唆するような公表やプレスリリースを、事前の書面承認なしに行ってはならない。** サイトやレポートに「Google公認」「Googleと連携」等の表現を書かない。

## 3. 実機確認の結果（2026-09-11・読み取りのみ）

`node --env-file=.env.local scripts/gbp/dump.mjs`

```
アカウント: 1 件
  accounts/100808672342933280965  川口メモリアル  (PERSONAL)
```

- **アカウント一覧の取得に成功した。** 承認前は割り当て0で 403 だったため、**承認が実際に効いていることを確認できた。**
- ただし次の段階で停止：

```
My Business Business Information API has not been used in project 487251905710
before or it is disabled.
```

**＝ Cloud Console 側で `My Business Business Information API` が未有効。**

## 4. 次にやること（松澤の作業・Cloud Console）

`scripts/gbp/README.md` の一覧に従い、プロジェクト `487251905710` で以下を有効化する。

```
My Business Account Management API      ← 有効化済みと推定（アカウント取得に成功したため）
My Business Business Information API    ← 未有効（エラーで確認）
My Business Q&A API
My Business Place Actions API
My Business Notifications API
My Business Verifications API
My Business Lodging API
Google My Business API                  ← 承認後にConsoleへ表示されるようになる
```

直リンク（エラーメッセージが案内したもの）：
`https://console.developers.google.com/apis/api/mybusinessbusinessinformation.googleapis.com/overview?project=487251905710`

有効化後、反映に数分かかる。そのうえで再度 `dump.mjs` を実行する。

## 5. 有効化後の進め方

**必ず dump → diff → apply の順で進める。順番を飛ばさない。**

```bash
node --env-file=.env.local scripts/gbp/dump.mjs     # 現在値の取得（読み取りのみ・差し戻し用バックアップ）
node --env-file=.env.local scripts/gbp/diff.mjs     # desired.mjs との差分確認（書き込みなし）
node --env-file=.env.local scripts/gbp/apply.mjs    # dry-run（1バイトも送信しない）
```

`apply.mjs --confirm` は**公開情報を書き換える操作**。内容を人が確認してからでなければ実行しない（`AGENTS.md`）。

### 適用前に確認すること

- 2026-09-07〜09-09 にブラウザから手作業で入れた内容（駐車場の属性訂正、口コミ2件への返信、無料相談・式場見学のサービス追加、説明文461字、事前相談の投稿、外観写真のカバー設定）が `desired.mjs` と食い違っていないか。**食い違ったまま apply すると手作業の結果を上書きする。**
  - 参照：`2026-09-07-gbp-implementation-status.md`、`2026-09-09-gbp-description-and-post-saved.md`、`2026-09-09-gbp-cover-and-save-status.md`
- `dump.mjs` の出力を先に保存し、差し戻せる状態にしてから進める

## 6. できるようになること

- **`performance.mjs` による月次のパフォーマンス取得**（読み取り専用）。これまで管理画面を目視するしかなかった GBP の数値を、月次で自動取得できる
  - 2026-08 のメール通知では「先月 135 人のユーザーが表示」（7月分）、6月分 104 人、5月分 116 人、4月分 113 人。API ではこれより細かい内訳が取れる
- 属性・サービス・説明文・投稿の状態を、ブラウザを開かずに検証できる
- 花ぜんセレモニーとの差がローカルパックにあるという分析（[[project-hanazen-competitive-position]]）に対し、**GBP側の実数で効果を測れるようになる**

## 7. 注意（据え置き）

- 承認されたのは**APIの利用**であって、掲載順位や表示に直接の効果はない
- `apply.mjs --confirm` は公開情報の変更。分析タスクのついでに実行しない
- API が使えるようになっても、**口コミ返信・投稿は引き続き人が内容を確認してから**

---

# 追記：API 有効化後の実機確認（2026-09-11）

松澤が Cloud Console で API を有効化 → 再実行。

## 8. スクリプト側に不具合があり、修正した

有効化後も `dump.mjs` が `400 Request contains an invalid argument` で停止した。
readMask のフィールドを1つずつ試して切り分けたところ、**`attributes` が原因**と判明。

`attributes` は Location リソースのフィールドではないため、`locations.list` の readMask に
含めると 400 になる。属性は専用エンドポイント
`info.locations.getAttributes({ name: "<location>/attributes" })` で取得する。

| ファイル | 修正内容 |
|---|---|
| `scripts/gbp/auth.mjs` | `READ_MASK` から `"attributes"` を削除。再発防止のコメントを追加 |
| `scripts/gbp/dump.mjs` | 属性を `getAttributes` で別途取得し、内容も一覧表示するようにした |

修正後、**全項目の取得に成功。**

## 9. 取得できた現在値（2026-09-11・読み取りのみ）

```
ロケーション: 1 件  locations/3951486682924761892
  名称           : 株式会社川口典礼 川口メモリアルホール
  メインカテゴリ : 葬儀屋
  追加カテゴリ   : 斎場 / 葬儀サービス
  電話           : 0120-963-765
  サイト         : https://kawaguchitenrei.com/     ← 公式サイトのまま維持されている
  説明文         : 461文字                          ← 9/09 に保存した内容が入っている
  サービス       : 5 件（事前相談・お見積り／川口メモリアルホール見学／葬祭業／斎場／葬儀店）
  属性           : 12 件
  営業時間       : 7 期間の設定あり
  Q&A            : 取得失敗（404）
```

- **ウェブサイト欄が `kawaguchitenrei.com` のまま維持されている**ことを API 側でも確認。
  2026-08-20 の連携解除の効果が続いている（却下理由Bの再発なし）
- **説明文461文字**＝2026-09-09 に保存した内容が公開側に入っている

### 属性12件は実態と整合していた（2026-09-11 松澤確認）

| 属性 | 値 | 判定 |
|---|---|---|
| `is_owned_by_women` | **true** | ✅ **正しい。川口典礼の社長は女性**（2026-09-11 松澤確認）。変更不要 |
| `welcomes_lgbtq` | true | ✅ 方針として妥当 |
| `is_transgender_safespace` | true | ✅ 方針として妥当 |
| `has_onsite_parking` / `has_parking_lot_free` | true | ✅ 駐車場70台・無料と整合 |
| `has_parking_lot_paid` / `has_parking_street_paid` / `has_parking_street_free` | false | ✅ 整合 |
| `has_wheelchair_accessible_seating` / `_restroom` / `has_restroom_unisex` / `has_onsite_services` | true | ✅ 整合 |

**属性の変更は不要。** 当初「実態と合っているか要確認」としたが、確認の結果すべて正しかった。

> 補足：`is_owned_by_women` は公開プロフィールに「女性経営」として表示される属性で、
> **事実に基づく正しい表示**。葬儀という商材ではご遺族への配慮という文脈で意味を持ちうるが、
> サイト側でどう扱うかは別途の判断（現状 `lib/company.ts` の `representative` は未設定で、
> 会社概要ページの「代表者」欄は条件分岐により非表示になっている）。

### ⚠ Q&A API は廃止済みだった（当初の記載を訂正）

当初このファイルに「`My Business Q&A API` を Cloud Console で有効化する」と書いたが、**これは誤りだった。**
松澤が有効化したうえで再検証した結果、次が判明した。

| 確認内容 | 結果 |
|---|---|
| `qanda.locations.questions.list` | 404（JSON ではなく HTML のエラーページ） |
| `https://mybusinessqanda.googleapis.com/$discovery/rest?version=v1` | **404** |
| 比較：`https://mybusinessbusinessinformation.googleapis.com/$discovery/rest?version=v1` | 200 |

**discovery ドキュメントごと 404 ＝ サービスのホスト自体が存在しない。** 当社の設定の問題ではない。

Google 公式の Deprecation schedule（`https://developers.google.com/my-business/content/sunset-dates`）:

| API | 廃止日 | サポート終了 |
|---|---|---|
| **My Business Q&A API** | **2025-11-03** | 2025-09-15 |
| My Business Business Calls API | 2023-05-30 | 2023-02-21 |

**Cloud Console で有効にしても動かない。** 一覧に残っていても実体がない。

### GBP の「質問と回答」機能そのものも終了している

API だけでなく、**GBP の Q&A 機能自体が 2025-12-03 から段階的に終了**しており、
2026年5月までに多くの日本の店舗で表示されなくなっている。
代替は Google マップの「この場所について質問」（Ask Maps・Gemini による自動回答）だが、
**日本では 2026年4月時点で未展開**。

### したがって Q&A 10問の行き先はサイト側

`desired.mjs` の `QANDA`（夜間対応・駐車場・火葬場の近さ・家族葬の費用・夕暮れ家族葬・見学・
市民葬・市外からの相談・事前相談・ペット同伴の10問）は **GBP へは適用できない。**

**内容は消さずに残し、用途を「サイトの `/faq/` とエリアページの `FAQPage` 構造化データの原稿」に変更した。**
当社サイトには既に `/faq/` があり、エリアページ16本すべてに `FAQPage` の構造化データが入っている
（2026-09-11 のSEOチェックで確認済み）。**受け皿は既にある**ので、内容の突き合わせをすればよい。

AI 検索時代の受け皿はサイト側にある、という整理は `CLAUDE.md` §10 の AIO 方針とも、
2026-09-11 に確認した「ChatGPT 経由の成果が突出（1セッションあたり Google 自然検索の約48倍）」とも
方向が一致する。

### スクリプトを直した（誤った案内を残さないため）

| ファイル | 修正内容 |
|---|---|
| `scripts/gbp/dump.mjs` | Q&A の 404 を「― （API廃止済み 2025-11-03。異常ではない）」と表示。廃止の経緯をコメントで残した |
| `scripts/gbp/diff.mjs` | 【Q&A】見出しに「API は廃止済み。適用できません」を明記 |
| `scripts/gbp/apply.mjs` | `--qanda` を付けても実行しないようにした。案内文から `--qanda` を削除 |
| `scripts/gbp/desired.mjs` | `QANDA` の用途を「サイト `/faq/` の原稿」と注記（内容は保持） |
| `scripts/gbp/README.md` | 有効化リストから `My Business Q&A API` を削除し、廃止済みである旨を追記 |

### カテゴリ検索スクリプトの表示は当てにしない

`dump.mjs` の「カテゴリ候補（『葬儀』で検索）」は、`filter: displayName=葬儀` が効いておらず
鍼師・空港・兵舎などの無関係なカテゴリを返す。`displayName=葬儀屋` でも牛丼屋・鍛冶屋が返る。
**このセクションの出力は無視してよい**（メインカテゴリは既に「葬儀屋」で設定済みのため実害なし）。
必要になったら別途フィルタ構文を調べる。

## 10. ⚠ `apply.mjs --confirm` を今のまま実行してはいけない

`diff.mjs`（読み取りのみ）を実行したところ、**`desired.mjs` が古く、手作業の結果を壊す内容**になっていた。

| 項目 | desired.mjs が行おうとすること | 判定 |
|---|---|---|
| **名称** | 「株式会社川口典礼 川口メモリアルホール」→ **「川口典礼」** | **❌ 実行しない。** 現在の名称は登記名＋看板名で正しい（[[project-three-brand-domains]]・[[project-gbp-api-access-request]]）。変更しないと決めてある |
| **説明文** | 461文字 → **493文字** | **❌ 実行しない。** 493文字版は 2026-09-09 に保存できず、電話行を削除した461字で保存成功した経緯がある（`2026-09-09-gbp-description-and-post-saved.md`）。戻すと同じ失敗を繰り返す |
| メインカテゴリ | 葬儀屋 → 葬儀店 | 🟡 要検討。変更の根拠が未確認 |
| 追加カテゴリ | 斎場／葬儀サービス → 葬儀場／葬祭業 | 🟡 要検討 |
| 営業時間 | 24時間営業（7日）に設定 | 🟡 要検討。現在は7期間の設定あり |
| サービス | 6プランを追加（直葬139,000／花入れ229,000／一日葬396,000／夕暮れ451,000／家族葬528,000／市民葬231,000） | 🟢 価格は `CLAUDE.md` §9 の正本と一致。追加自体は妥当 |
| サービス（既存） | 「葬祭業」「斎場」「葬儀店」が期待値にないと警告 | 🟡 カテゴリ名がサービスとして登録されている。整理の余地あり |
| Q&A | 10件追加 | ⏸ Q&A API が未有効のため現時点で実行不可 |

`diff.mjs` のガードレール事前チェック自体は「✅ 違反なし」だが、
**ガードレールは禁止表現のチェックであって、手作業との整合は見ていない。**

### 結論

**`desired.mjs` を現状に合わせて直すまで、`apply.mjs --confirm` は実行しない。**
最低限、①名称を現行のまま ②説明文を461文字版に の2点を直す必要がある。
カテゴリ・営業時間は変更の要否から判断する。

## 11. 残タスク

1. ~~`My Business Q&A API` を有効化~~ → **不要。2025-11-03 に廃止済み**（上記で訂正）
2. ~~`desired.mjs` を現状に合わせて修正~~ → **完了（2026-09-11）。下記「12.」参照**
   - `apply.mjs` の dry-run で `updateMask` に **`title` が入る**ことを確認済み。
     このまま `--confirm` すると名称が「川口典礼」に書き換わる
   - dry-run では「メインカテゴリ『葬儀店』の categoryId を解決できませんでした」とも出ており、
     カテゴリは結局書き込み対象から外れる。`desired.mjs` のカテゴリ指定自体が現状に合っていない
3. ~~属性3件の実態確認~~ → **完了。すべて正しく、変更不要**（2026-09-11 松澤確認）
4. そのうえで dump → diff → apply（dry-run）→ `--confirm` の順に進める
5. **`Business Profile Performance API` を Cloud Console で有効化する**（松澤）
   - 2026-09-11 に `performance.mjs` を実行したところ全項目 403。原因は当社設定ではなく**このAPIが未有効**
   - エラー本文：`Business Profile Performance API has not been used in project 487251905710 before or it is disabled`（`SERVICE_DISABLED`）
   - **名前が "My Business 〜" で始まらないため、有効化の一覧から漏れやすい。** `README.md` の一覧に追加した
   - 直リンク：`https://console.developers.google.com/apis/api/businessprofileperformance.googleapis.com/overview?project=487251905710`
   - 有効化後：`node --env-file=.env.local scripts/gbp/performance.mjs --from=2026-08-01 --to=2026-08-31`
6. `QANDA` の10問を `/faq/` とエリアページの FAQ に反映できているか突き合わせる（サイト側の作業）

---

## 12. `desired.mjs` の修正を実施（2026-09-11）

松澤の指示により、`apply.mjs --confirm` で手作業の結果を壊す2点を修正した。

| 項目 | 修正前 | 修正後 |
|---|---|---|
| `NAP.title` | `"川口典礼"` | **`"株式会社川口典礼 川口メモリアルホール"`**（現行値と同一） |
| `DESCRIPTION` | 493文字（末尾に「電話：0120-963-765 ／ 048-281-1117」の行あり） | **461文字**（電話行を削除。公開中の内容と完全一致） |

いずれも修正理由をファイル内のコメントに残した。電話番号は `NAP.primaryPhone` /
`additionalPhones` で別途設定されるため、説明文に重ねる必要もない。

### ガードレールも直した

名称を現行値に戻したところ、`guard.mjs` が
「名称を『株式会社川口典礼 川口メモリアルホール』に変更しようとしています」と違反判定した。
このチェックは `ALLOWED_TITLES`（川口典礼／株式会社川口典礼／株式会社 川口典礼）に
無い名前への**変更**を止めるためのもので、**現在値と同じ＝変更なしの場合まで止めるのは誤り**。

→ `checkPatch` で「現在の名称と同じなら通す」ように修正した。
キーワード詰め込みへの**変更**は引き続き止まる。

### 検証結果

```
diff.mjs
  【基本情報】 一致  名称 ／ 一致  ウェブサイト ／ 一致  電話（主）／ 一致  電話（副）
  【説明文】   一致  ビジネス説明文

apply.mjs（dry-run）
  ✅ ガードレール通過
  送信予定 title       : 株式会社川口典礼 川口メモリアルホール   ← 現行と同じ（実質変更なし）
  送信予定 description : 461文字                              ← 現行と同じ（実質変更なし）
```

**危険だった2点は解消。** ただし `--confirm` はまだ実行していない。

### 残っている差分（未承認・今回は触っていない）

| 項目 | 内容 | 状態 |
|---|---|---|
| カテゴリ | 葬儀屋 → 葬儀店／斎場・葬儀サービス → 葬儀場・葬祭業 | 🟡 要判断。なお dry-run では「『葬儀店』の categoryId を解決できませんでした」として**書き込み対象から自動的に外れている** |
| 営業時間 | 7期間 → 24時間営業（7日） | 🟡 要判断 |
| サービス | 5件 → 8件（6プラン追加。価格は §9 正本と一致） | 🟡 妥当だが未承認 |
| 既存サービス | 「葬祭業」「斎場」「葬儀店」がカテゴリ名のままサービス登録されている | 🟡 整理の余地あり |

**この4点を決めてからでないと `--confirm` は実行しない。**

---

# 13. 残り4点の整理（2026-09-11・松澤の指示で実施）

**結論：4点のうち実際に書き込みが必要だったのは1点だけだった。** 2点は「変更不要」で、
残り1点は書き込み前に内容の誤りが見つかったため修正してから実施した。

## 13-1. カテゴリ → **変更不要。`desired.mjs` が間違っていた**

日本のカテゴリを**全4,053件（100件×41ページ）取得して突き合わせた**結果：

| `desired.mjs` の指定 | 実在するか |
|---|---|
| 葬儀店（メイン） | **存在しない** |
| 葬儀場（追加） | **存在しない** |
| 葬祭業（追加） | **存在しない** |

実在する葬祭関連カテゴリは次の9件しかない。

```
葬儀屋              categories/gcid:funeral_director          ← 現在のメイン
斎場                categories/gcid:funeral_home              ← 現在の追加
葬儀サービス        categories/gcid:funeral_celebrant_service  ← 現在の追加
火葬業              categories/gcid:cremation_service
墓地                categories/gcid:cemetery
軍用墓地            categories/gcid:military_cemetery
ペット霊園          categories/gcid:pet_cemetery
ペット葬儀サービス  categories/gcid:pet_funeral_services
仏壇販売店          categories/gcid:butsudan_store
```

**現在の設定（葬儀屋／斎場・葬儀サービス）が、実在カテゴリの中で最適。** 変更しない。

- **火葬業は付けない。** 川口市めぐりの森は市営であり当社は火葬場を運営していない。
  付けると火葬場運営者と誤認される（`CLAUDE.md` の停止条件）
- **ペット系は付けない。** ペット葬儀への導線は文脈上ふさわしくない（`CLAUDE.md` §11）

→ `desired.mjs` の `CATEGORY_INTENT` を現状（実在カテゴリ）に修正。`forbidden` に
「火葬業」「霊園」「ペット」を追加した。

### 併せて `apply.mjs` のカテゴリ解決も直した

`categories.list` の `filter: "displayName=葬儀"` は効かず、鍼師・空港・牛丼屋などを返す。
そのせいでカテゴリが常に「解決できませんでした」になっていた。**全件取得して自前で突き合わせる**方式に変更。

## 13-2. 営業時間 → **すでに24時間営業だった。`diff.mjs` の誤検出**

実データは日〜土の7日すべてが `openTime: {} 〜 closeTime: {hours:24}` ＝ **既に24時間営業**。

`diff.mjs` が `p.openTime?.hours === 0` で判定していたが、**API は 0 時を
`openTime: {}`（既定値のため省略）で返す**ため `undefined` となり、毎回「→ 変更」と誤検出していた。
省略＝0 として扱うよう修正。**GBP 側の変更は不要だった。**

## 13-3 / 13-4. サービスの整理 → **実施（唯一の書き込み）**

`apply.mjs --confirm --only=serviceItems` を実行。`serviceItems` は全置換のため、
6プランの追加と不要3件の削除が同時に完了した。

### 書き込み前に見つけて直した3点

| # | 問題 | 対応 |
|---|---|---|
| 1 | **市民葬に「※表示は事前相談会員価格（税込）です」が付いていた** | 市民葬は「川口市民 葬祭事業価格」であって会員価格ではない。**誤った価格表示**（`CLAUDE.md` §14）なので `SHIMIN_PRICE_NOTE` を新設して出し分けた |
| 2 | 無料の2件にも価格の注記が付いていた | 無料サービスに「火葬料が別途…」は不自然。注記は有料プランのみに付けるようにした。文言は 2026-09 に手作業で入れた現行のものをそのまま採用 |
| 3 | `freeFormServiceItem.category` が未設定 | 既存の実データはすべて category を持っている。メインカテゴリ（葬儀屋）に紐付けた |

### API の制約で1回失敗した

初回の書き込みは 400 で失敗。詳細は次のとおり。

```
service_items[0..5].free_form_service_item.label.description
  "Strings must not contain leading/trailing or contiguous whitespace."
```

**サービス説明文に改行を入れられない。** 本文と注記を改行でつないでいたのが原因。
半角スペース1つでつなぎ、連続空白を1つに詰めて前後を削るよう修正して成功した。

### 書き込み後の状態（`dump.mjs` で確認）

```
サービス : 8 件
  - 直葬プラン            / 139,000円
  - 花入れお別れプラン    / 229,000円
  - 一日葬プラン          / 396,000円
  - 夕暮れ家族葬          / 451,000円
  - 家族葬プラン          / 528,000円
  - 川口市民葬プラン      / 231,000円
  - 事前相談・お見積り    / 無料
  - 川口メモリアルホール見学 / 無料
```

価格は `CLAUDE.md` §9 のプラン正本と一致。
**中身が空だった「葬祭業」「斎場」「葬儀店」の3件は消えた。**

### `diff.mjs` の最終結果

```
【基本情報】一致（名称・ウェブサイト・電話×2）
【カテゴリ】一致（メイン・追加とも）
【説明文】  一致
【営業時間】一致（24時間営業・7日）
【サービス】現在 8 件 → 期待 8 件（すべて「既存」）
【Q&A】     API 廃止済みのため適用不可（サイトの /faq/ 側で持つ）
```

**Q&A 以外の差分はゼロになった。**

## 13-5. 差し戻し用のバックアップ

- `tmp/gbp/backup-before-apply.json`（apply 実行時に自動生成）
- `tmp/gbp/backup-<日時>-before-serviceitems.json`（書き込み前に手動で複製）

いずれも `tmp/` 配下で git 追跡外。

## 13-6. この作業で直したスクリプト

| ファイル | 内容 |
|---|---|
| `desired.mjs` | カテゴリを実在するものに修正／市民葬の注記を分離／無料サービスの注記を外す／`category` を付与／改行を入れない |
| `apply.mjs` | カテゴリ解決を全件取得方式に変更 |
| `diff.mjs` | 24時間営業の判定（`openTime: {}` を 0 時として扱う） |
| `guard.mjs` | 名称が現在値と同じ（＝変更なし）なら通す |

---

# 14. 2つのAPIを有効化 → 全機能が使えるようになった（2026-09-11）

松澤が Cloud Console で `Business Profile Performance API` と `Google My Business API` を有効化。
実測で4系統すべて 200 を確認した。

| 確認項目 | 結果 |
|---|---|
| Business Profile Performance（通話数など） | ✅ 200 |
| v4 口コミ（reviews） | ✅ 200 |
| v4 投稿（localPosts） | ✅ 200 |
| v4 写真（media） | ✅ 200 |

> 補足：`Google My Business API` は**アクセス承認が下りるまで Cloud Console のライブラリに表示されない**ため、
> 承認前は有効化しようがなかった。承認（2026-09-10）後に初めて有効化できた。
> また `Business Profile Performance API` だけ名前が "My Business 〜" ではないため、一覧から漏れやすい。

## 14-1. 取れるようになったもの

**月次の実数**（`performance.mjs`）：マップ表示・検索表示（PC／スマホ別）・**通話数**・
サイトクリック・ルート検索・メッセージ・予約。6か月さかのぼって取得できた。
→ **`docs/operations/gbp/2026-09-11-gbp-monthly-log.md` に月次ログを新設した。**

**口コミ・投稿・写真**（v4）：取得と、（必要なら）返信・投稿・アップロードも可能になった。

## 14-2. 最初の取得で分かった重要なこと

### 通話が 7月13件 → 8月4件（−69%）。マップ表示の減少と連動している

| 月 | マップ表示 | 検索表示 | 通話 |
|---|---:|---:|---:|
| 2026-07 | 695 | 789 | **13** |
| 2026-08 | 541（−22%） | 1,052（+33%） | **4**（−69%） |

検索表示は増えているのに通話は落ちた。**地図枠の露出と電話の本数が連動している**という見立てで、
「花ぜんとの差はローカルパック」という 2026-08-07 の分析と方向が一致する。
ただし一桁の数字なので断定せず、9月・10月を積んでから判断する。

### 口コミは万全

28件・平均4.5・**未返信0件**。★1と★2の低評価にも返信済み。

### 投稿に方針と逆の表現が残っていた

- **2025-03-31 の投稿に「格安葬儀」が入ったまま公開中。** 「格安」は 2026-08-03 の判断で
  広告の除外キーワードに登録した語（[[project-no-price-seeker-acquisition]]）であり、
  `CLAUDE.md` §11 のトーンにも合わない。**削除または差し替えを検討する**
- 2024-11-19 の投稿が `REJECTED`（Google に拒否）のまま残っている
- 2026-02-09 → 2026-07-30 の約5.5か月が空白。**投稿は単発では効かない**

### 写真は15枚と少ない

カバー1・プロフィール1・その他13。§13 の制約（遺影・名札・会葬礼状・ご参列者は不可）の範囲で、
式場内観・駐車場・面会室・外観などを増やす余地がある。

## 14-3. GBP 側の残タスク（更新）

| # | 内容 | 誰が |
|---|---|---|
| 1 | ~~Business Profile Performance API の有効化~~ | ✅ 完了 |
| 2 | ~~Google My Business API の有効化~~ | ✅ 完了 |
| 3 | 「格安葬儀」を含む 2025-03-31 の投稿の扱いを決める | 要判断 |
| 4 | REJECTED の投稿（2024-11-19）を削除するか決める | 要判断 |
| 5 | 投稿の頻度を決める（いまは単発） | 要判断 |
| 6 | 写真の追加（撮影できる範囲で） | 要判断 |
| 7 | 月初に `performance.mjs` を回して月次ログへ1行追加 | 運用 |
| 8 | `My Business Q&A API` は廃止済みなので Cloud Console で無効化してよい | 任意 |

---

# 15. 投稿の差し替え・削除と写真の追加（2026-09-11・松澤の指示で実施）

## 15-1. 2025-03-31 の投稿を差し替えた ✅

「格安葬儀」を含む本文を全面的に書き直し、`localPosts.patch`（`updateMask=summary`）で更新した。
更新直後は `PROCESSING`（Google の再審査中）だったが、確認時点で **`LIVE` に戻っている**。

**差し替え前に含まれていた問題（3点）**

| 表現 | 問題 |
|---|---|
| 「**格安葬儀**」 | 2026-08-03 の判断で広告の除外キーワードに登録した語。価格で選ぶ層を追わない方針と逆（[[project-no-price-seeker-acquisition]]）。`guard.mjs` の `BANNED_PHRASES` にも入っている |
| 「明確な料金設定」 | 総額が確定しているかのように読める。`CLAUDE.md` §14 に照らして不適切 |
| 「業者です」「最適なプランを提案」 | 葬儀社としてのトーンに合わない／断定寄り（§11） |

**差し替え後の本文（364文字・禁止表現0件を機械チェック済み）**

```
川口典礼は、埼玉県川口市西新井宿の葬儀社です。2006年の創業から20年、川口市・新井宿・鳩ヶ谷を
中心に、年間約260件のご葬儀のお手伝いをしてまいりました。累計の施行実績は4,600件以上になります。

自社式場「川口メモリアルホール」は、川口市営の火葬場「川口市めぐりの森」まで車で約5分。敷地内に
無料駐車場を70台ご用意しており、ご家族だけの家族葬から200名規模の一般葬まで対応いたします。
個室の面会室もございます。

直葬・火葬式、一日葬、家族葬、川口市の市民葬まで、ご希望とご事情に合わせてお選びいただけます。
プランに含まれる内容と、別途必要になる費用についても、お見積りの際にご説明いたします。

ご相談・お見積りは無料です。お急ぎのご連絡も、まだ何も決まっていない段階のご相談も、
24時間365日承っております。
```

数字（創業20年・年間約260件・4,600件以上・車で約5分・70台・200名）はすべて `lib/company.ts` と
`CLAUDE.md` §10 の正本どおり。価格は出していない（価格で選ぶ層を追わない方針のため）。

## 15-2. 2024-11-19 の REJECTED 投稿を削除した ✅

「新事業ネクストフラワーセレモニー」の投稿を `localPosts.delete` で削除。投稿は 12件 → **11件**。

> 削除してよかった理由がもう1つあった。**本文の末尾に担当者の個人名が記載されていた。**
> `REJECTED` で公開されていなかったため実害はなかったが、公開されていれば `CLAUDE.md` §12 に触れる内容だった。

## 15-3. 写真を3枚追加した ✅

本サイトで使用している素材から、**内容を1枚ずつ目視で確認したうえで**アップロードした（`media.create` / `sourceUrl` 方式）。

| 追加した写真 | カテゴリ | 確認した内容 |
|---|---|---|
| `hall-interior.jpg` | INTERIOR | 玄関ホール・階段・受付。人物なし |
| `hall-ceremony-room.jpg` | INTERIOR | 式場。祭壇と椅子のみ。**遺影・名札・参列者なし** |
| `hall-family-waiting-room.jpg` | INTERIOR | 親族控室（和室）。人物なし |

写真は 15枚 → 18枚 → **19枚**（下記②を含む）。

### ⚠ 採用しなかった2枚と、その理由

**① `hall-parking.png` — Google ストリートビューのスクリーンショットだった**

画像に **「©2020 Google」のウォーターマークが写り込んでいる。** Google 自身の著作物であり、
これを自社写真として Google ビジネスプロフィールにアップロードするのは適切でない。
`CLAUDE.md` §13「他社サイト・素材サイトの画像を無断使用しない」にも抵触する。

**この画像は本サイトでも使われている**（要対応）。

```
app/lp/page.tsx:56        image: "/images/home/hall/hall-parking.png"
app/lp/page.tsx:119       alt: "敷地内の駐車場"
components/home/ThreeStrengthsSection.tsx:35
```

トップページの「3つの強み」と広告LPの両方で「敷地内の駐車場」として使われている。
**駐車場70台は当社の主要な訴求点なので、実際に撮影した写真への差し替えを提案する。**
撮影できれば GBP にも追加できる。

**② `kawaguchi-memorial-hall-visitation-room-pet.png` → 松澤の判断により掲載した ✅**

個室面会室でペットと一緒にお別れする様子の写真。棺と人物（喪服）が写っているため、
`CLAUDE.md` §13 の「掲載前に必ず人間が確認」に該当すると判断して一旦保留し、判断を仰いだ。
**2026-09-11、松澤の指示により INTERIOR カテゴリで追加した。**

写真は 18枚 → **19枚**（COVER 1／PROFILE 1／INTERIOR 4／ADDITIONAL 13）。

> 個室面会室でペットと一緒にお別れできることは当社の特徴で、本サイトでも掲載している内容。
> なお `CLAUDE.md` §11 のとおり、ペット葬儀・ペット火葬への導線としては扱わない（面会の様子として掲載するに留める）。

## 15-4. 実施後の状態

| 項目 | 実施前 | 実施後 |
|---|---|---|
| 投稿 | 12件（うち REJECTED 1件・方針違反 1件） | **11件・すべて LIVE・方針違反なし** |
| 写真 | 15枚 | **19枚** |
| 口コミ | 28件・平均4.5・未返信0 | 変更なし |

## 15-5. 残タスク

1. **`hall-parking.png` の差し替え（実写待ち・2026-09-11 松澤が後日撮影すると回答）**
   - 現状：ストリートビュー画像。2026-09-11 に松澤がウォーターマーク（「©2020 Google」×2か所）を消したが、
     **写真自体はストリートビューの撮影画像のまま**（構図・1958×803の切り出し幅が消す前と一致）。
     著作権表示を消しても権利がGoogleにある事実は変わらず、これを GBP に上げると
     **Googleの撮影画像を自社の店舗写真としてGoogle自身に提出する**形になるため、**GBPへのアップロードは保留した**
   - ウォーターマークを消した版は**ローカルで未コミット**。本番にはまだ出ていない
     （本番は現在もウォーターマーク入りの旧版が表示されている状態）
   - **後日、松澤が実写を撮影して渡す**とのこと（2026-09-11 合意）
   - 受領後の手順：`public/images/home/hall/hall-parking.png` に**同じファイル名で上書き**する。
     ファイル名・配置を変えなければ**コード修正は不要**（`CLAUDE.md` §13）。
     反映先は `components/home/ThreeStrengthsSection.tsx:35`（トップの「3つの強み」）と
     `app/lp/page.tsx:56, 119`（広告LP）の2か所。あわせて GBP へ EXTERIOR カテゴリで追加する
2. ~~ペット同伴の面会室の写真を GBP に載せるかの判断~~ → **完了。掲載した（2026-09-11）**
3. 投稿の頻度を決める（直近は 2026-09-08 の1本。2026-02〜07 に5.5か月の空白があった）
