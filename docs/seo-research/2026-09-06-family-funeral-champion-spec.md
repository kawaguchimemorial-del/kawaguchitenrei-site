# 修正仕様書：家族葬の主ページ（champion）をトップに確定する

- **状態**：**松澤の承認待ち。承認後に Opus が実装する**
- **作成**：2026-09-06 ／ 根拠：`docs/reports/2026-09-06-family-funeral-champion-8expert.html`
- **目的**：「川口市 家族葬」「川口 家族葬」で、トップページと `/plan/family-funeral/` が同じ看板（title・h1・アンカー）を掲げて競合している状態を解消し、Google が実際に選んでいるトップページを正式な主ページにする
- **やらないこと**：新規ページ作成、エリアページの title 変更、構造化データの追加、価格・プラン名の変更、デザインの刷新

---

## 0. 前提として知っておくこと（Opus 向け）

| 日付 | 何があったか |
|---|---|
| 2026-06-30 | 家族葬ページ強化。サイト全体の内部リンクのアンカーを「川口市の家族葬」→ `/plan/family-funeral/` に統一（フッター含む） |
| 2026-07-05 | 家族葬の主ページ＝トップに決定。トップ title に「家族葬」 |
| 2026-07-14 | **反転**。主ページを `/plan/family-funeral/` へ。トップ title から「家族葬」を除去。「ユーザー確認事項」のまま未確定 |
| 2026-07-14〜09-03 | 7週間、Google は「川口市 家族葬」でトップを出し続けた（28日窓：トップ365表示・10.9位、専用ページ5表示・77.8位）。反転は Google に受け入れられなかった |
| 2026-09-06 | Codex がトップ title に「家族葬」を戻す（`d3fc44b`）。経緯を知らずに行ったが方向は正しい。ただし専用ページの title は「川口市の家族葬｜…」のままなので**カニバリが復活** |

**結論：Google の選択（トップ）に合わせる。専用ページは「プランの料金・内容」の役割に分ける。**

---

## 1. 変更するファイルと内容

### 1-1. `lib/plans.ts` ― `family-funeral` の看板を「プラン料金・内容」に変える

対象は `slug: "family-funeral"` のオブジェクト（207行付近〜）。**価格・人数・本文・FAQ は触らない。**

| フィールド | 現在 | 変更後 |
|---|---|---|
| `subtitle` | `"川口市の家族葬"` | `"家族葬プラン"` |
| `heroTitle`（＝h1） | `"川口市の家族葬プラン"` | `"家族葬プランの料金と内容"` |
| `metaTitle` | `"川口市の家族葬｜1日1組の自社式場・費用と流れ｜川口典礼"` | `"家族葬プランの料金と内容｜10〜30名・1日1組の川口メモリアルホール｜川口典礼"` |
| `metaDescription` | `"川口市で家族葬をお考えの方へ。1日1組貸切の川口メモリアルホールで、通夜・告別式を行います。10〜30名が目安、事前相談会員価格528,000円(税込)・通常628,000円(税込)。火葬料・飲食等は別途。人数や総額の目安を24時間365日ご相談いただけます。"` | `"川口典礼の家族葬プラン。事前相談会員価格528,000円(税込)・通常価格628,000円(税込)、参列10〜30名が目安。1日1組貸切の川口メモリアルホール（川口市西新井宿）で通夜・告別式を行い、川口市めぐりの森で火葬します。含まれる内容、別途かかる費用、総額の考え方をご案内します。"` |
| `image.alt` | `"川口市の家族葬プランのイメージ"` | `"家族葬プランのイメージ"` |

- `conclusionBox.heading`「川口市で家族葬を検討している方へ。」は**そのまま**（本文の地域言及は自然な範囲）
- 価格は `pricing: { member: 528000, regular: 628000 }` を変えない。description の数字は CLAUDE.md §9 と一致していることを確認する
- 変更後、`metaTitle` が60字以内であることを確認する（上記案は 47字）

### 1-2. 内部リンクのアンカー ― 「川口市の家族葬」→「家族葬プランの料金と内容」

現在、サイト全体で **136本** の内部リンクが `川口市の家族葬` というアンカーで `/plan/family-funeral/` を指している。これは 6/30 に専用ページを主ページにする目的で統一したもの。主ページをトップに戻す以上、このアンカーは逆向きの信号になる。

**変更するもの（`/plan/family-funeral/` へ向くリンクのアンカーのみ）：**

| ファイル | 箇所 | 変更後のアンカー |
|---|---|---|
| `components/column/ColumnLocalFuneralGuide.tsx` | 22行 `label: "川口市の家族葬"` | `"家族葬プランの料金と内容"` |
| `components/home/AreasSection.tsx` | 65行 | 同上 |
| `components/home/MeguriSection.tsx` | 24行 `name` | 同上 |
| `components/home/PlanSection.tsx` | 78〜79行 `ctaLabel` / `name` | `name: "家族葬プラン"`、`ctaLabel: "料金と内容を見る"` |
| `components/plan/PlanDetailBody.tsx` | 539行 | `"家族葬プランの料金と内容"` |
| `lib/plans.ts` | 539・696・1026・1186・1734行の `label` | 同上 |
| `lib/plans.ts` | 877行 `name` | 同上 |
| `app/area/*/page.tsx` | 各エリアの `label: "川口市の家族葬"`（angyo・aoki 他、全エリア） | 同上 |
| `lib/columns.ts` | 「川口市の家族葬」8件のうち、`/plan/family-funeral/` へのリンクのもの | 同上。**本文中の地の文は変えない** |

**変更しないもの：**

- **`components/layout/Footer.tsx` 9行**（`{ label: "川口市の家族葬", href: "/plan/family-funeral/" }`）。**§5 の保護対象**のため、この仕様書では触らない。フッターは全ページに出るので136本の大半はここ。**松澤が別途承認した場合のみ** `"家族葬プランの料金と内容"` へ変更する。承認がなければ残す（残っても、他の変更で方向は十分に揃う）
- `/plan/family-funeral/` 以外へ向くリンク（例：`/area/kawaguchi/` への「川口市の葬儀」）

作業は `grep -rn "川口市の家族葬" app/ components/ lib/` で一覧を取り、**href が `/plan/family-funeral/` のものだけ**を対象にする。

### 1-3. トップページ ― 家族葬の見出しを1本立てる

現在、トップ（`app/page.tsx`）の h2 に「家族葬」を含むものは **0本**（本文では53回出現）。主ページにする以上、見出しで宣言する。

**新規コンポーネント `components/home/FamilyFuneralSection.tsx` を作り、`app/page.tsx` の `<PlanSection />` の直後に置く。**

内容（§3 のデザイン方針に従い、カードを並べず読み物として）：

```
<section className="bg-white py-16 md:py-24">
  <div className="mx-auto max-w-6xl px-5 md:px-8">
    <div className="max-w-3xl">
      <p ...eyebrow>Family Funeral</p>
      <h2>川口市の家族葬は、1日1組の自社式場で。</h2>
      <p>ご家族やごく親しい方を中心に、通夜と告別式を行うお別れです。
         参列は10〜30名が目安。川口市西新井宿の川口メモリアルホールは1日1組の貸切で、
         川口市めぐりの森まで車で約5分です。</p>
      <p>事前相談会員価格 528,000円（税込）／通常価格 628,000円（税込）。
         火葬料・お料理・返礼品などは別途となります。</p>
      <p>
        <Link href="/plan/family-funeral/">家族葬プランの料金と内容</Link>
        <Link href="/hall/kawaguchi-memorial-hall/">川口メモリアルホールの設備</Link>
      </p>
    </div>
  </div>
</section>
```

- 価格は **`lib/plans.ts` の `getPlan("family-funeral")` から取得**する（ハードコードしない。`PlanSection` は現状ハードコードだが、新規分は従わない）
- **他の形式（直葬・一日葬・市民葬）を貶める比較を書かない**（§11・§15）。家族葬だけを推す文体にしない。「ご家族の状況に合わせて」の一文を入れる
- 「1日1組」「駐車場70台」「めぐりの森まで車で約5分」は `lib/halls.ts` / `lib/company.ts` の値と一致させる
- 既存のトーン（セリフ体見出し・余白・白背景）を守る。`ThreeStrengthsSection` や `MeguriSection` の書き方に合わせる
- スマホでの文字量が過剰なら段落を2つに削る

### 1-4. `app/page.tsx` ― title・description は変えない

Codex が 9/06 に入れた title「川口市の葬儀・家族葬｜自社式場・24時間365日対応｜川口典礼」を**追認**する。description（家族葬528,000円を含む）もそのまま。

### 1-5. `CLAUDE.md` §10 ― 主ページを1行で固定する

§10.2 の重要キーワード一覧の直後に追記：

```
### 10.4 主ページ（champion）の固定

- **「川口市 葬儀」「川口 葬儀」「川口市 家族葬」「川口 家族葬」の主ページはトップページ（`/`）。**
  `/plan/family-funeral/` はプランの料金・内容の受け皿で、地域検索の主ページにしない。
  title・h1・内部リンクのアンカーで、トップと専用ページに同じ「川口市の家族葬」を掲げない。
  （2026-07-14 に一度反転させ、7週間で Google に受け入れられなかったため 2026-09-06 に確定。
  経緯は `docs/reports/2026-09-06-family-funeral-champion-8expert.html`）
```

---

## 2. 実装の順序

1. `lib/plans.ts` の 1-1（看板の変更）
2. 1-2 のアンカー変更（Footer.tsx を除く）
3. 1-3 の新規セクション
4. `npm run build` → 静的生成140ページ・sitemap 129 URL を確認
5. ビルド出力で確認（下記 §3）
6. 1-5 の CLAUDE.md 追記
7. `docs/reports/` に作業レポート（`skills/work-report/SKILL.md`）
8. commit。**push は §19.1 の「既存ページの SEO/AIO 改善」に該当するため自走可**。ただし **Footer.tsx を触った場合は push 前に承認**

---

## 3. 確認項目（ビルド出力で機械的に）

```
node -e "
const fs=require('fs');
const strip=h=>h.replace(/<script\b[^>]*>[\s\S]*?<\/script>/g,'');
const top=strip(fs.readFileSync('.next/server/app/index.html','utf8'));
const fam=strip(fs.readFileSync('.next/server/app/plan/family-funeral.html','utf8'));
const h2=h=>[...h.matchAll(/<h2[^>]*>([\s\S]*?)<\/h2>/g)].map(m=>m[1].replace(/<[^>]+>/g,'').trim());
console.log('トップ title:',(top.match(/<title>([^<]*)</)||[])[1]);
console.log('トップ 家族葬を含むh2:',h2(top).filter(x=>x.includes('家族葬')));
console.log('専用 title:',(fam.match(/<title>([^<]*)</)||[])[1]);
console.log('専用 h1:',(fam.match(/<h1[^>]*>([\s\S]*?)<\/h1>/)||[])[1]?.replace(/<[^>]+>/g,''));
console.log('専用 title に「川口市の家族葬」:',/川口市の家族葬/.test((fam.match(/<title>([^<]*)</)||[])[1]));
"
```

期待値：

| 確認 | 期待 |
|---|---|
| トップ title | 「川口市の葬儀・家族葬｜…」（変更なし） |
| トップの「家族葬」を含む h2 | **1本以上**（新規セクション） |
| 専用ページ title | 「家族葬プランの料金と内容｜…」で、**「川口市の家族葬」を含まない** |
| 専用ページ h1 | 「家族葬プランの料金と内容」 |
| `/plan/family-funeral/` へのアンカー「川口市の家族葬」の残数 | Footer.tsx 由来のみ（承認なしの場合）。それ以外は0 |
| 静的生成 | 140ページ |
| sitemap | 129 URL |
| 価格 | 528,000 / 628,000 がトップ・専用ページ双方に表示 |

加えて `npm run lint` で新規ファイルにエラーがないこと（内部リンクは `<Link>` を使う）。

---

## 4. 効果判定

- **判定日：2026-09-20 前後**（変更から2週間）
- **見るもの**：GSC 7日窓で「川口市 家族葬」「川口 家族葬」の平均順位と受け皿
- **効果あり**：「川口市 家族葬」が **13位以内**（8/31週 17.4 → 1ページ目下端）。受け皿はトップのまま
- **効果なし**：17位前後のまま。その場合は文言で動く範囲を超えていると判断し、GBP（地図枠）に集中する。専用ページ側へ戻す再反転は**しない**
- **副作用の監視**：「川口市 葬儀」「川口 葬儀」が 8〜10位から落ちないこと。落ちたらトップの新規セクションの文字量を削る

---

## 5. 触ってはいけないもの（再掲）

- `components/layout/Footer.tsx`・`Header.tsx`・`MobileBottomCTA.tsx`（§5）
- `lib/areas.ts` の各エリア `metaTitle`（7/05 判断：地名＋家族葬のロングテールは保護）
- 価格・プラン名（§9）
- `app/lp/**`（別案件）
