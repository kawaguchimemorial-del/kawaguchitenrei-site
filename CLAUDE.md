@AGENTS.md

# 川口典礼サイト — Claude Code 作業ルール

このファイルは Claude Code が本プロジェクトで作業する際のルール。
以下の方針に従わない作業は、たとえ技術的に正しくても受け入れない。

---

## 1. プロジェクトの目的

埼玉県川口市の葬儀社「**川口典礼**」の公式サイト。
ご家族が葬儀社を検討する最初の接点として、**信頼感・地域密着・問い合わせ導線・SEO/AIO** を重視する。
派手な演出よりも、落ち着いた読み物として安心して読めることを優先する。

---

## 2. 技術スタック

- **Next.js** (App Router) — `AGENTS.md` の通り、訓練データと差分がある前提で `node_modules/next/dist/docs/` を確認する
- **TypeScript**
- **Tailwind CSS**
- **Vercel** にデプロイ
- **スマホファースト** — ブレークポイントは `md:` から上に積む形で書く

---

## 3. デザインの方向性

- **上品・余白重視・読み物寄り**
- 既存のトーン＆マナー（落ち着いたセリフ体見出し、ブランドカラーは差し色程度、白背景＋柔らかい境界線）を尊重する
- カードを並べる訴求より、**語りかける・読ませる**構成を優先する
- 既存セクションに手を入れる場合、上記の方向性を踏襲する

### デザイン暴走防止

- **サイト全体のデザインコンセプトを勝手に変更しない**
- 既存ページとのトーン統一を優先する（局所最適より一貫性）
- 「よりモダンに」「より高級に」「より今っぽく」などの理由で全体 UI を変更しない
- 改善提案がある場合は、まず別チャットで議論し、合意を取ってから着手する

---

## 4. CTA / 導線ポリシー

- スマホ下部固定 CTA は **「電話」と「事前相談」の2つのみ**
- **LINE 導線は今は入れない**（将来追加の可能性はあるが、指示があるまで実装しない）
- **自動見積もりシステム（`funeral-system`）の完全統合は後回し** — リンクや動線追加は最小限にとどめる

---

## 5. 触ってはいけないもの

以下は **明示的な指示がない限り変更しない**:

- `funeral-system/` 配下一式
- `package.json`（依存追加・スクリプト変更・バージョン変更すべて含む。必要があれば必ず事前提案）
- `components/layout/Header.tsx`
- `components/layout/Footer.tsx`
- `components/layout/MobileBottomCTA.tsx`

これらに変更が必要だと判断した場合も、勝手に編集せず、**まず提案して承認を得る**こと。

---

## 6. 作業の進め方（重要）

1. **作業前に短い実装計画を出す**
   - 何を、どのファイルで、どう変えるか
   - 数行で十分。先に合意を取る
2. **1回の作業では指示されたファイルだけ編集する**
   - 巻き込み変更、周辺リファクタ、命名統一などは別タスク扱い
   - 「ついでに直しておきました」はしない
3. 不確実な点は手を動かす前に質問する
4. **編集後は、変更ファイル一覧と確認コマンドを必ず提示する**
   - 変更したファイルのパス一覧
   - 確認コマンド:
     - `npm run dev`
     - `npm run build`
   - 必要なら確認用 URL（例: `http://localhost:3000/area/kawaguchi`）
5. **Recap は短く簡潔にする**
   - 長文の振り返り・自己評価・章立ての要約は書かない
   - 「変更したファイル」「次のアクション」だけに絞る
   - 質問された内容には素直に答え、頼まれていない総括をしない

---

## 7. コンテンツ・SEO 優先度

新規ページ・既存ページ改善は、以下の優先順で進める:

1. **`/area/*`** — 地域SEOを最優先（川口市内・近隣地域のロングテール獲得）
2. **`/saijo/*`** — 斎場ページは次点
3. **legal pages**（利用規約・プライバシーポリシー等）は **最後**

SEO/AIO の観点では、構造化データ・見出し階層・地名×サービス語の自然な配置を意識する。

---

## 8. 困ったときの判断基準

迷ったら以下の順で優先する:

1. **ご遺族への配慮**（言い回し、ビジュアル、煽らない）
2. **信頼感・地域密着**
3. **問い合わせ動線への接続のなめらかさ**
4. **SEO / AIO**
5. 実装の優雅さ

---

## 9. 料金・プラン名の正本

外部サイトを都度参照せず、以下を正本とする。

- 直葬プラン：事前相談会員価格 139,000円（税込） / 通常価格 189,000円（税込）
- 花入れお別れプラン：事前相談会員価格 229,000円（税込） / 通常価格 279,000円（税込）
- 一日葬プラン：事前相談会員価格 396,000円（税込） / 通常価格 496,000円（税込）
- 家族葬プラン：事前相談会員価格 528,000円（税込） / 通常価格 628,000円（税込）
- 市民葬プラン：川口市民 葬祭事業価格 231,000円（税込）

プラン名・金額は勝手に変更しない。不明点は推測せず確認する。

---

## 10. SEO / AIO の中心軸

このサイトの SEO / AIO は、以下の「軸」で組み立てる。
迷ったら、これらの軸に沿うかどうかで判断する。

### 10.1 中心軸（5つの柱）

1. **地域**：川口市・新井宿・鳩ヶ谷・近隣エリア
2. **自社式場**：川口メモリアルホール（駐車場70台、家族葬〜200名対応）
3. **火葬場近接**：川口市めぐりの森まで車で約5分
4. **葬儀形式**：直葬・火葬式・一日葬・家族葬・一般葬・市民葬
5. **対応力**：24時間365日相談対応、創業20年、年間約260件の施行実績

### 10.2 重要キーワード（SEO/AIO 共通）

- 川口 葬儀 / 川口市 葬儀
- 川口 家族葬 / 川口市 家族葬
- 川口 直葬 / 川口市 直葬
- 川口 火葬式 / 川口市 火葬式
- 川口 一日葬 / 川口市 一日葬
- 川口 市民葬 / 川口市 市民葬
- めぐりの森 葬儀 / 川口市めぐりの森 葬儀
- 新井宿 葬儀 / 新井宿 家族葬
- 鳩ヶ谷 葬儀 / 鳩ヶ谷 家族葬

これらは「ページ全体に自然に分散」させる。タイトル・h1・h2・本文・FAQ・パンくず・構造化データに散りばめる。
**キーワードの羅列・連呼は禁止。**

### 10.3 3 軸判断基準

すべての改修は次の 3 点で判断する。1 つでも明確に外れる施策は採用しない。

1. **検索される**：検索ユーザーが探しやすくなるか
2. **AI に引用される**：AI 検索・生成AI回答に拾われやすい形式か（結論先出し・基本情報表・FAQ・構造化データ）
3. **問い合わせにつながる**：CTA への動線がなめらかか

---

## 11. 葬儀社サイトとしてのトーン

詳細は `docs/05-content-guidelines.md` を参照。要点だけここに置く。

- **落ち着き・誠実さ・地域密着** を最優先
- **不安を煽る表現禁止**：「今すぐ決めないと損」「絶対に最安」「後悔しない唯一の方法」など
- **断定の禁止**：「必ず」「絶対」「最安保証」など
- **押し売り禁止**：CTA は誘導しすぎず、相談ハードルを下げる文体に
- **競合の誹謗中傷禁止**：他社名を出すときも事実と公開情報の範囲に限定
- **ペット同伴の扱い**：個室面会室での同席として自然に触れる範囲に留める。ペット葬儀・ペット火葬への導線は文脈上ふさわしくないため面会セクションには入れない

---

## 12. 個人情報・フォーム・Webhook ルール

詳細は `docs/04-privacy-review.md` を参照。常時遵守事項：

- `.env` / `.env.local` / その他環境変数ファイルは **読まない・表示しない・編集しない・コミットしない**
- `FORM_WEBHOOK_SECRET`・API キー・Webhook URL などの秘密情報は **絶対にコードやログに出さない**
- 個人情報（氏名・電話・メール・住所・相談内容・故人情報・葬儀日程など）は **ログ出力しない**
- 施工事例・口コミ・画像に **個人名・故人名・喪主名を出さない**
- 遺影・名札・会葬礼状などが写る画像は **掲載前に必ず人間が確認**
- フォーム改修時は `docs/04-privacy-review.md` のチェックリストを実行
- Google Apps Script Webhook 連携・バリデーション・エラー表示を壊さない

---

## 13. 画像取り扱いルール

- 既存画像のファイル名・配置場所は変更しない（リネーム禁止）
- Next/Image を使用する
- alt は内容を具体的に記述（「川口メモリアルホールの外観」「個室面会室でペットと一緒にお別れする様子」など）
- 他社サイト・素材サイトの画像を無断使用しない
- 寺院会館・民営式場の写真を無断掲載しない

---

## 14. 価格表記ルール

- 税込・税別の区分が確認できない金額に勝手に「税込」と書かない
- 「目安」と表記し、「正式なお見積り時に確認」を併記
- 「必ず追加費用なし」「絶対に最安」など断定しない
- プラン正本（このファイルの §9）以外の数字は推測しない

---

## 15. 競合比較ルール

- 競合他社の固有名詞を出すときは事実・公開情報のみ
- 誹謗中傷・優劣の断定はしない
- 「大手ポータル型」「互助会型」「地元葬儀社型」などカテゴリ比較は可
- 詳細は `docs/02-competitor-analysis-summary.md` 参照

---

## 16. 作業後の確認ルール

1. `npm run build` を実行し、エラー・警告がないことを確認
2. `git status` で変更ファイルを確認
3. 変更ファイル一覧と確認 URL を提示
4. コミット・push は明示的に指示されたときだけ実行
5. 破壊的操作（`git reset --hard`、`git push --force`、`rm -rf`、ファイルリネーム、依存削除など）は事前承認なしに実行しない

---

## 17. ドキュメント参照先

| トピック | ファイル |
|---|---|
| プロジェクト全体方針 | `docs/00-project-policy.md` |
| SEO / AIO 方針 | `docs/01-seo-aio-policy.md` |
| 競合分析 | `docs/02-competitor-analysis-summary.md` |
| 改修ロードマップ | `docs/03-improvement-roadmap.md` |
| Privacy Review | `docs/04-privacy-review.md` |
| 文言・コンテンツ方針 | `docs/05-content-guidelines.md` |
| 作業用プロンプト集 | `docs/prompts/claude-code-prompts.md` |
| SEO/AIO 評価チェックリスト | `docs/eval/seo-aio-checklist.md` |

不確実な点があったら、まずこれらの文書を参照してから着手する。
それでも判断がつかない場合は、推測せず手を止めて確認する。

---

## 18. Claude Code に期待する出力形式

- 作業前：短い実装計画（数行）
- 作業中：必要最小限のコメント。長文の途中報告はしない
- 作業後：
  - **変更したファイル**：パス一覧
  - **確認コマンド**：`npm run dev` / `npm run build`
  - **確認 URL**：該当ページ
  - **コミット**：明示指示があれば実行・なければ未実行と明記
- 不要な要約・自己評価・章立ては書かない

---

## 19. 自動 push / push 前確認の範囲

詳細は `docs/ai-workflows/guardrails-and-approval.md` を参照。要点を以下に記す。

### 19.1 自動 push してよい（自走 push 可）

明示指示があれば、以下の範囲は build / git status / 安全確認をパスした時点で commit → push まで実施してよい:

- `docs/**` の追加・更新（特に `docs/eval/records/`・`docs/operations/`・`docs/ai-workflows/`・`docs/seo-research/`）
- 新規エリアページ追加（`app/area/*/page.tsx` の新規作成・`lib/areas.ts` への新規追加）
- 既存ページの SEO/AIO 改善（title / metaDescription / FAQ / JSON-LD / relatedLinks / Conclusion ボックス / 結論先出し補強）
- 画像配置（`public/images/**` への追加。**`public/images/tmp/tmp.txt` 除く**）
- sitemap への新規エリア追加（純粋な追加のみ、既存変更なし）
- Search Console 作業ログ更新（URL 検査・公開 URL テスト・インデックス登録リクエストの記録）
- 本番確認記録
- 構造化データの新規追加（**ただし Review / aggregateRating / ratingValue は除く**）
- 文言修正（既存トーンを維持する範囲、価格・式場利用可否を変更しない範囲）
- build 成功済みの通常 SEO/AIO 改善

### 19.2 push 前確認必須（人間承認なしで push しない）

以下のいずれかに該当する場合、commit までは進めてよいが、**push は人間承認後に行う**:

- フォーム関連の改修（`app/contact/**`・`app/estimate/**`・`app/api/**`）
- Webhook / `.env*` / `FORM_WEBHOOK_SECRET` / API キー / credentials / secrets
- `package.json` / lockfile / `next.config.ts`
- `components/layout/Header.tsx` / `Footer.tsx` / `MobileBottomCTA.tsx`
- 価格データの新規追加・変更（§9 プラン正本以外の数字を出す場合）
- 寺院会館・民営式場・公営斎場の利用可否・空き状況・宗派条件・檀家条件の新規断定
- `funeral-system/` 配下
- Review / aggregateRating / ratingValue を含む JSON-LD の新規追加
- 個人情報（個人名・故人名・喪主名・遺影が写る画像）が含まれる可能性のある追加
- 破壊的操作（`git reset --hard` / `git push --force` / `rm -rf` / ファイルリネーム / 依存削除）

### 19.3 危険時の停止条件

以下のいずれかに該当した場合、commit / push 前に**作業を停止して人間に確認を求める**:

- build エラー / TypeScript エラー / 静的生成 131 ページが減る
- `.env*` の差分が出ている
- `public/images/tmp/tmp.txt` への接触
- 川口典礼が運営していない施設（川口市めぐりの森・寺院会館・民営式場・戸田葬祭場・谷塚斎場）を自社運営のように見せる表現
- 禁止表現（最安 / 必ず / 絶対 / 追加費用なし / 総額確定 / 標準価格 / いつでも利用 / 空きがあります / 確実に手配 など）の混入
- 想定外に大きな差分（指示外ファイルへの巻き込み変更）

---

## 20. Skills / Agents 参照先

このファイル（CLAUDE.md）は**入口・憲法**として最重要ルールに絞る。具体手順・チェックリストは Skill 側、エージェント役割は agents/ 側に分離する。

| 用途 | 参照先 |
|---|---|
| Skill / Agent / Prompt / Hook / 人間承認の分類 | `docs/ai-workflows/skill-index.md` |
| SEO/AIO ページ改善の標準手順 | `skills/seo-page-improvement/SKILL.md` |
| Search Console 作業ログ手順 | `skills/search-console-log/SKILL.md` |
| 日本向け Privacy Review 手順 | `skills/privacy-review-jp/SKILL.md` |
| 6 段階コードレビュー手順 | `skills/code-review-6-stages/SKILL.md` |
| 各エージェント定義 | `agents/*.md`（research / review / documentation / privacy-reviewer / memory-curator） |
| メモリ管理ルール（チャット情報の保存先振り分け） | `docs/ai-workflows/memory-management.md` |
| AI 運用ロードマップ | `docs/ai-workflows/ai-operations-roadmap.md` |
| ガードレールと承認フロー | `docs/ai-workflows/guardrails-and-approval.md` |
| タスク分類 | `docs/ai-workflows/task-classification.md` |
| 評価指標 | `docs/ai-workflows/eval-metrics.md` |

不確実な点があったら、まず該当 Skill / Agent / docs を参照してから着手する。
それでも判断がつかない場合は、推測せず手を止めて確認する。
